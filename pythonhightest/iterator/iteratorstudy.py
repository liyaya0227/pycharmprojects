"""
生成器：
    注意：如果一个函数中有yield语句，那么这个就不再是一个函数了，而是一个生成器的模版；同理，调用该函数的时候就不是执行该函数，而是创建一个生成器对象。
1、同一个生成器创建的不同对象，调用不同对象互不影响
2、如果生成器带有返回值，可以用捕获异常的方式，在异常的结果里面用e.value来获取返回值
3、可以使用生成器对象调用.send(value)，那么yield的返回值就是send（）里面的值
4、send一般不会放在第一次启动生成器，如果非要这么做，传None
5、send的值当作yield的返回值
6、使用yield可以实现多任务
7、用协程async实现多任务操作
"""